spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cms_prod
    username: cms_user
    password: password
    platform: mysql
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQLDialect
    database: mysql
    show-sql: false